import { UltraVisualEngine } from '@/components/ultra-visual-engine';
import '../visual-engine.css';

export const metadata = { title: 'Experience' };

export default function ExperiencePage() {
  return <UltraVisualEngine />;
}
