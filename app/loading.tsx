export default function Loading() {
  return (
    <section className="page-shell empty-state" aria-busy="true" aria-live="polite">
      <p className="eyebrow">Loading</p>
      <h1>Getting the sports story ready.</h1>
      <p>Please wait while UltraWear loads the latest available content.</p>
    </section>
  );
}
